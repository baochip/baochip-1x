# Running Header Generation

Run `python3 ./rtl_to_svd.py --path ../../` from this directory.

Check the diff of the `output/bao1x_peri.svd` before committing to make sure there are no unexpected regressions on register extraction.